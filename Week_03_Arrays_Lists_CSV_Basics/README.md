## Week 03 — Arrays, Lists & CSV Basics

```markdown
# Week 03 — Arrays, Lists & CSV Basics

This week introduces working with external data — reading from and writing to CSV
files — alongside more advanced list operations including 2D arrays. You will also
need the sample data files from the `data/` folder at the root of the repo.

## Tasks

| File | Task | Key Concept |
|------|------|-------------|
| `01_CSV_Writer.py` | Write structured data to a CSV file | File I/O, `csv` module |
| `02_Dice_Roll.py` | Simulate dice rolls and record frequency distribution | Lists, randomness |
| `03_2D_Array_Adder.py` | Add two 2D arrays together element by element | 2D arrays, nested loops |
| `04_Temp_Stats_CSV.py` | Read temperature data from CSV and calculate statistics | CSV reading, data processing |
| `05_File_word_Search.py` | Search a text file for a target word and report occurrences | File reading, string search |

## Data Files
Tasks 04 and 05 use data files from the `data/` folder at the repo root. Make sure
you have cloned the full repo so these are available locally.

## AQA Spec Links
- Arrays (1D and 2D)
- File handling — reading and writing
- String operations
```