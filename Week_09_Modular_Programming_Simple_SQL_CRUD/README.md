## Week 09 — Modular Programming & Simple SQL CRUD

```markdown
# Week 09 — Modular Programming & Simple SQL CRUD

This week focuses on writing well-structured, modular code — breaking programs into
functions and separating concerns — alongside full CRUD (Create, Read, Update,
Delete) operations in SQL.

## Tasks

| File | Task | Key Concept |
|------|------|-------------|
| `01_inventory_system.py` | Build a modular inventory system with add, remove, and search functions | Modular design, functions |
| `02_File-Backed_Address_Book.py` | An address book that persists data to a file using modular functions | File I/O, modular programming |
| `03_Simple_SQL_CRUD.py` | Implement all four CRUD operations on a SQLite database | SQL CRUD, `sqlite3` |

## What is CRUD?
| Operation | SQL Command |
|-----------|-------------|
| Create | `INSERT INTO` |
| Read | `SELECT` |
| Update | `UPDATE` |
| Delete | `DELETE FROM` |

## AQA Spec Links
- Subroutines, functions, and procedures
- Modular programming
- SQL — full CRUD operations
```