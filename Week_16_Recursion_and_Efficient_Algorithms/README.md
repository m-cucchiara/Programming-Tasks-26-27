## Week 16 — Recursion & Efficient Algorithms

```markdown
# Week 16 — Recursion & Efficient Algorithms

This week focuses on recursion — functions that call themselves — applied to
increasingly complex problems. Recursive thinking is a key part of the AQA
spec and appears in both the theory and practical papers.

## Tasks

| File | Task | Key Concept |
|------|------|-------------|
| `01_mergesort.py` | Implement merge sort recursively (revisited with full recursive understanding) | Recursion, merge sort |
| `02_maze_solver.py` | Solve a maze using recursive backtracking | Recursion, backtracking |
| `03_n_queens_solver.py` | Solve the N-Queens problem using recursive backtracking | Recursion, constraint satisfaction |

## The Anatomy of a Recursive Function
Every recursive function needs:
1. A **base case** — the condition that stops the recursion
2. A **recursive case** — where the function calls itself with a smaller problem

Without a base case, you get infinite recursion and a `RecursionError`.

## AQA Spec Links
- Recursion — base case, recursive case
- Merge sort
- Problem solving and computational thinking
```